## samaypanwar.com

Simple landing page for `samaypanwar.com` with links to GitHub, LinkedIn, and notes.

### Develop

- Open `index.html` directly in a browser or use any static file server.

### Pre-commit hooks

```bash
poetry install
poetry run pre-commit install
poetry run pre-commit run --all-files
```
